Aureon - Minimal Angular-like Framework (TypeScript only)
This package contains:
- cli/ : TypeScript CLI to start backend and frontend dev servers
- app-server/ : Express backend in TypeScript
- app-client/ : Frontend in TypeScript, esbuild-based, Angular-like features (router, DI, components)
